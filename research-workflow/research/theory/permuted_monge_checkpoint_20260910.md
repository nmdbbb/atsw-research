# Quota checkpoint: permuted Monge screen remains independently unreviewed

Date: 2026-09-10. User authorized continued research with reserved time for push.
The reviewer returned a usage-limit error after this screen executed. No further
agent task, quota workaround, solver run or scope expansion was attempted.
Root stops research and publishes this resumable checkpoint. Exact remaining
account quota is not observable; no claim of stopping before every quota limit.

## Bounded question and result

Does a shared SVD order reveal Monge structure missed by scalar-state order on
the remaining general-support transport problems? Simultaneously permuting the
cost matrix and corresponding marginal coordinates preserves local OT. A passed
exact dyadic Monge predicate then permits the same numerical NW dispatch, with
all earlier floating arithmetic limitations retained.

The frozen posthoc screen used the same two T5/n128/seed1000/shift0 cases.
Three arms: original support order; then full-layer shared SVD orders in both
right orientations; finally additional private support-matrix SVD when shared
orders failed. Identity Monge is attempted first in all arms. Binary and forced
cases retain the same baseline; failed general checks use the same LP fallback.

| Case | Remaining LP calls: identity / shared / shared+private | Seconds in same order |
|---|---|---|
| Coarse AR1 k1 delta0.5 squared | 164 / 164 / 161 | 0.404 / 0.417 / 0.438 |
| Fine nonlinear k2 delta0.18 absolute | 298 / 293 / 287 | 0.833 / 0.846 / 0.898 |

Shared order saves 0% and 1.68% of remaining LP calls, below the frozen 20% on
each case gate. Private reordering also adds checks/work; no speed claim follows
from these single fixed-order timing repeats. All continuation tables agreed
with the same-run identity arm within 1e-8, and roots matched archived values.
Whole screen: 3.841 seconds. Root inspected the support-dispatch sequence,
permutation/marginal pairing, copied matrix lifetime, gates and source pins.

## Status and resume instructions

**Provisional result, independent implementation review pending.** The reviewer
failed with an explicit usage-limit error and delivered no new review for this
screen. Previous binary/Monge/defect/scale audits were independently reviewed;
this result must not inherit that approval. Do not rerun frozen artifacts or
promote a claim merely because numerical checks passed.

Next session: verify clean-checkout pins and tests, read
`status.orchestrator.json`, then independently review only
`probes/permuted_monge_screen.py` and its manifest/results. No benchmark rerun
is needed unless that review finds a concrete defect. If supported, close this
ordering route at its failed gate and choose a distinct mechanism on general
supports with a root-certificate/work prediction. Do not extend the current
priority/cache/ordering searches simply by increasing budgets or adding orders.

Artifacts: `runs/cycle_2/permuted_monge_screen/{manifest,results}.json`.
The original objective remains incomplete; no outward root certificate, novelty,
population certificate, full-grid comparison or SOTA win has been established.
