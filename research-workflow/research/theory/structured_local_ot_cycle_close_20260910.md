# Structured local OT cycle closed after independent review

Date: 2026-09-10. The user refilled quota and requested completion of the current
bounded cycle. The quota pause is resolved; the final independent review supports
closing the screened SVD-ordering route. No benchmark rerun was necessary.

**Cycle complete; original scientific objective incomplete.** The outcome is
`direction_stopped_evidence_based` for the declared structural skipping/reuse
routes and their tested scope. It is not a general lower-bound theorem, a
population certificate, a full-domain result, or a SOTA claim.

## Final review of the previously pending experiment

The independent reviewer inspected the permuted-Monge implementation, pinned
baseline, exact represented-matrix predicate, numerical NW helper, recorded
results and all 12 source pins. The general-support iterator follows the actual
baseline dispatch sequence; cost rows/columns and marginal permutations align.
The same scalar target and original initial-law root are retained. Both recorded
initial laws are deterministic, so empirical root coverage is limited accordingly.

| Case | Actual LP calls: original order / shared SVD / shared plus local SVD | Shared reduction |
|---|---|---:|
| AR1, k1, delta0.5, squared | 164 / 164 / 161 | 0% |
| Nonlinear, k2, delta0.18, absolute | 298 / 293 / 287 | 1.68% |

Both fail the frozen 20%-per-case reduction gate. Maximum recorded continuation
table discrepancy is 8.89e-16, below the declared 1e-8 numerical check. Single
fixed-order timings show no reason to promote the extra ordering work, but do
not establish a general runtime ranking. Actual LP calls are in `dispatch.LP_calls`;
the inherited `counts.LP_calls` counts entries to the general dispatch hook.

The Fraction predicate validates exact Monge inequalities of represented costs.
It does not repair floating marginal/NW arithmetic or certify a root interval.
Tiny-interior-mass and extreme-arithmetic limitations from the earlier review
remain explicit. These limitations do not prevent the scoped failed-gate decision.
No frozen source, manifest, result or old quota checkpoint was edited.

Independent evidence: [final review](permuted_monge_final_review_20260910.md).
The previous [quota checkpoint](permuted_monge_checkpoint_20260910.md) remains
historical and is superseded for current status by this closure record.

## What the completed cycle retains and closes

Retain the direct binary-support formula and strict local-Monge dispatch as
numerical engineering baselines. On the two small cases they reduce general LP
calls and match numerical reference tables, but they do not establish novelty
or outward certification. The independent full-horizon support census at frozen
sample sizes prevents extrapolating the much stronger tiny-sample sparsity.

Close the tested identical-support binary-order cache, broad strict-Monge
promotion, the 1e-10 near-Monge rescue premise, and this shared SVD ordering route
at their failed gates. Private SVD ordering supplies no shared-work contribution.
The [cycle evidence report](structured_local_ot_decision_20260910.md) records the
earlier results and their numerical/provenance limits.

## Next cycle entry condition

A new cycle needs a distinct mechanism on general supports, specifying the
operations omitted or shared, the cost of detecting and maintaining that
structure, and a valid route to original-root upper/lower control. Its comparator
must include the retained numerical dispatch baselines. Do not extend failed
priority/cache/ordering variants by increasing budgets, adding more orders, or
opening the development/confirmation grid without such evidence.

Closure validation: 85 unit tests passed; legacy pins remain 24 matching plus one
historical; the final screen's 12 source pins and result-manifest link match.
Only one fresh-context reviewer was used for the missing independent check.
The research objective remains marked false; the cycle's completion is tracked
separately in `status.orchestrator.json` and the new ledger-inbox decision.
